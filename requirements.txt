# ============================================================
# AsyncRAGSystem - 依赖清单
# 支持100人异步并发的RAG检索问答系统
# ============================================================

# --- Web框架 (异步高性能) ---
fastapi==0.115.0
uvicorn[standard]==0.30.0

# --- 向量数据库 (Milvus SDK 3.0) ---
pymilvus==3.0.0.0

# --- 异步HTTP客户端 (访问Ollama API) ---
httpx==0.27.0

# --- 数据校验与配置管理 ---
pydantic==2.9.0
pydantic-settings==2.5.0

# --- 文件上传支持 ---
python-multipart==0.0.9

# --- 异步文件操作 ---
aiofiles==24.1.0

# --- 环境变量加载 ---
python-dotenv==1.0.1

# --- Redis 缓存 (异步客户端) ---
redis>=5.0.0

# --- Nginx 配置 (部署层, 无需Python包, 配置文件在 nginx/ 目录) ---

